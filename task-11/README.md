# 🧩 Task 11 - Remote Data Dropdown

A React component that dynamically fetches user data from a remote API and displays it inside a controlled <select> dropdown. The component demonstrates real-world frontend patterns such as asynchronous data fetching, loading states, error handling, and controlled form inputs.
